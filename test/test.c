#include "../lib/bench.c"
#include "../lib/mmath.h"
// #include <stdbit.h>
#include <stdlib.h>
#include <string.h>

// u8 parityu8(u16 x) { return __builtin_parityg(x); }
typedef f32 vec2[2];

int main(void) {

  // union {f16 f;u16 u;} i;

  // // printu128(powu64(5,5));
  // for (i.u = 1;i.u;++i.u){
  //   printf16(i.f);
  //   printf("\t%e\n",(f64)i.f);
  // }

  // u16 i = 1;
  // for (i = 1; i; i <<= 1) {
  //   printu8n(__builtin_ffs(i));
  // }

  // printu128n(__builtin_powi(5,5));
}
